// ./src/apis/user/adaptor/out/user.repository.ts
// This file is generated by a shell script.
import { Injectable } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { iUserRepositoryPort } from '../../application/port/out/user.repository.port';

@Injectable()
export class UserRepository implements iUserRepositoryPort {
    constructor(
        @InjectRepository(ExampleEntity)
        private readonly exampleRepository: Repository<ExampleEntity>,
    ) {}
}
